# 已验证模板的参数覆盖

先匹配已验证模板，再把需要改变的参数以 `name/value/unit` 形式提交。系统会检查参数是否存在、单位是否一致以及数值是否在模板允许范围内，并生成独立约束 JSON，原始模板不会被修改。

示例：`T_hot=423.15 K`、`V_left=2 V`、`p_load=2e6 Pa`。

可调用 `/api/templates/derive-and-queue` 将派生文件直接提交本地 COMSOL 队列；本项目已验证热端温度覆盖后的二维热应力模型可以成功生成 MPH。
